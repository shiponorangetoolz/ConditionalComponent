import { Routes, Route, Link } from "react-router-dom";
import { Button } from 'react-bootstrap';

function Form({title}) {
  return (
    <div>
      <h6>{title}</h6>
    </div>
  );
}

export default Form;